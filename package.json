{
  "name": "@onre/evm",
  "version": "0.0.0",
  "private": true,
  "description": "OnRe EVM diamond contracts, built and deployed with Gemforge",
  "license": "MIT",
  "scripts": {
    "build": "gemforge build",
    "test": "gemforge build && forge test -vvv",
    "coverage": "gemforge build && forge coverage",
    "deploy:local": "gemforge deploy local",
    "deploy:testnet": "gemforge deploy testnet",
    "deploy:mainnet": "gemforge deploy mainnet",
    "query:testnet": "gemforge query testnet",
    "query:mainnet": "gemforge query mainnet",
    "verify:testnet": "gemforge verify testnet",
    "verify:mainnet": "gemforge verify mainnet",
    "devnet": "anvil",
    "admin:contracts": "gemforge build && forge build admin-ui/contracts/LocalAssetToken.sol",
    "admin:dev": "pnpm admin:contracts && vite admin-ui",
    "admin:build": "pnpm admin:contracts && vite build admin-ui",
    "test:integration": "bash scripts/test-gemforge-integration.sh"
  },
  "devDependencies": {
    "dotenv": "^16.6.1",
    "gemforge": "2.18.0",
    "solhint": "^6.2.3",
    "vite": "^6.4.3"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.15.4",
  "pnpm": {
    "patchedDependencies": {
      "gemforge@2.18.0": "patches/gemforge@2.18.0.patch"
    }
  },
  "dependencies": {
    "viem": "^2.55.19"
  }
}
